---
interface Props {
    text: string,
    classNames: string
}
const {text, classNames} = Astro.props
---


<button class=`flex justify-center items-center px-10 py-3 rounded-full ${classNames}` style="background: rgba(0,0,0,0.4)">
    <slot />
    {text}
</button>